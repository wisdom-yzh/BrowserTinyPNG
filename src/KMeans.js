const {
  GreyPixel,
  GreyAlphaPixel,
  IndexedPixel,
  RGBPixel,
  RGBAPixel
} = require('./Pixel');

/**
 * kMeans algorithm
 * @param {Array(Pixel)} pixelArray 
 * @param K
 * @return {Array(Pixel)} 
 */
const kMeans = (pixelArray, K) {
  
}

module.exports = kMeans;
