const {
  GreyPixel,
  GreyAlphaPixel,
  IndexedPixel,
  RGBPixel,
  RGBAPixel
} = require('./Pixel');

/**
 * kMeans algorithm
 * @param {Array(Pixel)} pixelArray 
 * @param K
 * @return {Array(Pixel)} 
 */
const kMeans = (pixelArray, K) {
  
}

class Cluster {

	/**
	 *
	 */
	constructor(pixels) {

	}

	/**
	 * 
	 */

	/**
	 * return cluster and create a palette
	 * @param int size (1~255)
	 * @return {Array} {pixels, palette}
	 */
	getPalette(size) {
		return this.palette;
	}

	/**
	 *
	 */
	getIndexedPixels() {
		return this.pixels;
	}

	/**
	 *
	 */	
	
	
}
module.exports = kMeans;
